{
  "name": "interactive-svg",
  "version": "1.0.0",
  "description": "A demo repo for scripts in inline SVG",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 8080 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bsmth",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
